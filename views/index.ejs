<%- include('layout.ejs') %>

<div class="text-center mt-5">
    <h1>Bienvenido al Sistema de Votación</h1>
    <p>Inicia sesión para participar en la votación.</p>

    <% if (!user) { %>
        <a href="/auth/google" class="btn btn-primary">Iniciar Sesión con Google</a>
    <% } else { %>
        <a href="/dashboard" class="btn btn-success">Ir al Dashboard</a>
    <% } %>
</div>
