<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Sistema de Votación' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center" href="/">
                <% if (user) { %>
                    <img src="/images/pm.jpg" alt="Logo" height="40" class="me-2">
                <% } else { %>
                    <img src="/images/pm.jpg" alt="Logo Desconectado" height="40" class="me-2">
                <% } %>
                <span>Sistema de Votación</span>
            </a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav ms-auto">
                    <% if (user) { %>
                        <li class="nav-item"><a class="nav-link" href="/dashboard">Dashboard</a></li>
                        <% if (user && user.role === 'master') { %>
                            <li class="nav-item"><a class="nav-link" href="/admin">Admin</a></li>
                        <% } %>
                        <!-- <li class="nav-item"><a class="nav-link" href="/auth/logout">Cerrar Sesión</a></li> -->
                    <% } else { %>
                        <li class="nav-item"><a class="nav-link" href="/auth/google">Iniciar Sesión con Google</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Contenedor del contenido dinámico -->
    <!-- <div class="container mt-4">
        <% if (typeof body !== 'undefined') { %>
            <%- body %>
        <% } else { %>
            <p class="text-center text-danger">Error: No se ha proporcionado contenido.</p>
        <% } %>
    </div> -->
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            if (window.location.pathname === "/logout") {
                window.location.href = "/";
            }
        });
    </script>
</body>
</html>
